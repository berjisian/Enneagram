angular.module('rtmap').directive('sortableLayerPanel', function() {	return {		restrict : 'EA',		templateUrl : 'app/lib/rtmap/directives/layerPanel/sortableLayerPanelTemplate.html',		scope : {			map: "="		},		controller : function($scope) {			$scope.layers = $scope.map.getLayers().getArray();						$scope.sortableOptions = {			    out: function(e, ui) {			    	//TODO bring back when item moved to out of box			    }, 			    			    stop: function(e, ui) {			    	$scope.map.render();			    }			}		},		link : function(scope, element, attrs, ctrls) {}	};});